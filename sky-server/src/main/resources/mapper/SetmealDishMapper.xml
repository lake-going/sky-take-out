<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">

    <insert id="insertSetmeal">
        insert into setmeal_dish(
        setmeal_dish.id,
        setmeal_dish.setmeal_id,
        setmeal_dish.dish_id,
        setmeal_dish.name,
        setmeal_dish.price,
        setmeal_dish.copies
        values
        <foreach collection="setmealDishList" item="setmealDish" separator=",">
            (null,
            #{setmealDish.setmealId},
            #{setmealDish.dishId},
            #{setmealDish.name},
            #{setmealDish.price},
            #{setmealDish.copies})
        </foreach>
    </insert>
    <delete id="deleteBySetmealId">
        delete from setmeal_dish sd where sd.setmeal_id = #{id}
    </delete>

    <select id="queryByDishId" resultType="com.sky.entity.SetmealDish">
        select * from setmeal_dish where dish_id = #{id}
    </select>
</mapper>
